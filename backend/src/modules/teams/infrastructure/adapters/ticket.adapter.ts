export class TicketAdapter {

    async assignTicket(ticketId: string, teamId: string) {

        // integração futura com módulo tickets
        console.log(`Assign ticket ${ticketId} to team ${teamId}`);
    }
}